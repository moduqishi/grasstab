import React from 'react';
import ReactDOM from 'react-dom/client';
import GrassTabLanding from './index.tsx';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GrassTabLanding />
  </React.StrictMode>,
);

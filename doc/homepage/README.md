# GrassTab 发布页

这是 GrassTab 的官网发布页，独立于主项目。

## 文件结构说明

```
GrassTab/                         # 主项目根目录（新标签页扩展）
├── App.tsx                       # 主应用
├── index.tsx                     # 扩展入口
├── components/                   # 扩展组件
├── manifest.json                 # 扩展配置
└── doc/
    └── homepage/                 # 🌐 发布页（独立项目）
        ├── index.tsx             # 发布页主组件
        ├── main.tsx              # React 入口
        ├── index.html            # HTML 模板
        ├── styles.css            # 样式文件
        ├── package.json          # 独立依赖
        └── vite.config.ts        # 独立构建配置
```

## 快速开始

```bash
# 进入发布页目录
cd doc/homepage

# 安装依赖
npm install

# 开发模式
npm run dev
# 访问 http://localhost:3001

# 构建（相对路径，可直接打开）
npm run build

# 构建（GitHub Pages，绝对路径）
npm run build:github

# 预览构建结果
npm run preview
```

## 构建模式

### 1. 本地/直接打开（默认）
```bash
npm run build
```
- 使用相对路径 `./`
- **⚠️ 注意**：由于使用 ES6 模块，不能直接双击 HTML 打开
- 需要通过 HTTP 服务器访问

**本地查看方式：**
```bash
# 方式 1：使用 npm preview
npm run preview
# 访问显示的地址

# 方式 2：使用 Python HTTP 服务器
cd dist
python -m http.server 8000
# 访问 http://localhost:8000

# 方式 3：使用 VS Code Live Server 扩展
# 右键 dist/index.html -> Open with Live Server
```

### 2. GitHub Pages 部署
```bash
npm run build:github
```
- 使用绝对路径 `/grasstab/`
- 适合部署到 GitHub Pages
- 自动化部署工作流会使用此命令

## 技术栈

- React 19
- Framer Motion（动画）
- Tailwind CSS（样式）
- Lucide React（图标）
- TypeScript + Vite

## 特性

- 🎨 现代化设计
- ✨ 流畅动画效果
- 📱 完全响应式
- 🚀 极速加载
- 🤖 AI 功能展示
- 🖼️ 沉浸式滚动预览

## 部署

发布页会自动部署到 GitHub Pages：
- 推送代码到 main 分支
- GitHub Actions 自动构建
- 访问：https://moduqishi.github.io/grasstab/

## 注意

⚠️ 此项目与主项目（新标签页扩展）完全独立：
- **不共享依赖**：有自己的 `package.json`
- **不共享配置**：有自己的 `vite.config.ts`
- **不互相影响**：修改互不影响
